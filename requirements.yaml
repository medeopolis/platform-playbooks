---
collections:
  # Required for debops
  - name: ansible.netcommon
  # DebOps for management
  - name: 'https://github.com/debops/debops#/ansible/'
    type: git
    version: "stable-3.0"
  # For managing resources in Openstack API compatible clouds
  - name: openstack.cloud
    version: ">=2.0.0"
  # For managing resources at Vultr
  - name: vultr.cloud
    version: ">=1.5.0"
  # Misc resources
  - name: community.general
  # Used for managing grafana resources
  - name: grafana.grafana
  # To interact with IPAM
  - name: netbox.netbox
  # For AWS management
  - name: amazon.aws

# roles:
#   - name: semaphore
#     src: 'https://github.com/morbidick/ansible-role-semaphore.git'
#     type: git

